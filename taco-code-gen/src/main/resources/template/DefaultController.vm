package ${packageName};


import ${controllerTemplatePackage}.${controllerTemplateName};
import ${serviceImplPackage}.${serviceClassName};
import ${dtoPackage}.${dtoName};
import ${criteriaPackage}.${criteriaClassName};
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/${resourcePath}")
public class ${controllerClassName} extends ${controllerTemplateName}<${dtoName}, ${criteriaClassName}> {

    protected ${controllerClassName}(${serviceClassName} service) {
        super(service);
    }
}

